{ 
  "listenAddress": "{{ .listen_address }}",
  "proxyBackend": {
    "baseUrl": "{{ .proxy_backend_base_url }}",
    "backendPrefix": "{{ .proxy_backend_prefix }}",
    "token": "{{ .proxy_backend_token }}"
  },
  "bluecat": {
    "account": "{{ .bluecat_account }}",
    "baseUrl": "{{ .bluecat_api_baseUrl }}",
    "username": "{{ .bluecat_api_username }}",
    "password": "{{ .bluecat_api_password }}",
    "configurationId": "{{ .bluecat_configurationId }}",
    "viewId": "{{ .bluecat_viewId }}"
  },
  "cidrFile": "{{ .cidr_file }}",
  "token": "{{ .token }}",
  "logLevel": "{{ .log_level }}",
  "org": "{{ .org }}"
}
